# STUDY.md — 친절한 학습 가이드 (한국어)

수식·코드보다 먼저 **직관**을 잡기 위한 안내서입니다. 노트북(`notebook/motor_dynamics.ipynb`)을
옆에 두고, 각 단계에서 이 문서의 해당 절을 먼저 읽으세요. (영문 스펙은 `README.md`, 깊은 수학적
근거는 `NOTES.md`.)

---

## 0. 이 프로젝트가 답하는 질문 한 줄
> **"팔을 뻗을 때 운동피질 뉴런들은 어떻게 함께 움직이며, 그 패턴은 *왜* 생기는가?"**

- **어떻게 보이나** (기술, descriptive) → **jPCA**
- **왜 그런가** (기전, mechanistic) → **고정점 분석(fixed points)**

이 둘의 차이가 프로젝트 전체를 지배합니다. 아래에서 천천히 쌓아갑니다.

---

## 1. 준비물: '신경 상태 공간'이라는 그림
- 뉴런이 N개라면, **매 순간**을 "N개의 발화율"로 이루어진 **N차원 공간의 점 하나**로 봅니다.
- 시간이 흐르면 그 점이 움직이며 **궤적(선)**을 그립니다. 팔 방향(조건)마다 다른 궤적.
- Churchland(2012)의 놀라운 발견: 이 궤적들을 **딱 맞는 2D 평면**에서 보면 다들 **원을 그리며
  회전**합니다. 이것을 **회전 동역학(rotational dynamics)**이라고 부릅니다.
- 👉 노트북 **M2**의 그림에서 직접 확인: 108개 조건이 부채처럼 회전합니다.

---

## 2. jPCA = "회전이 보이는 각도"를 찾아주는 자동 카메라 (기술)

**비유.** 방 안에서 사람들이 제각각 움직이는데, 천장에서 내려다보니 다들 회전목마처럼 돌고
있더라 — jPCA는 그 **'천장 시점(평면)'**을 데이터에서 **자동으로** 찾아줍니다.

**작동 직관.**
1. 각 순간의 **속도**(어디로 얼마나 움직이나 = `Ẋ`)를 **현재 위치**(`X`)로 설명하는 규칙을 찾습니다:
   `속도 = M × 위치`.
2. 단, `M`을 **'순수 회전만 하는 행렬'(반대칭, skew-symmetric)**로 제한합니다.
   - 일반 행렬 = 늘이기 + 회전이 섞임.
   - 반대칭 행렬 = 늘이기 0, **오직 빙글빙글 회전**.
3. 그래서 jPCA는 "이 데이터에 회전 성분이 얼마나 있나?"를 **정직하게** 잽니다. 억지로 회전을
   넣는 게 아니라, 회전만 남겨서 재는 것.

👉 노트북 **M0의 🔍 셀**(`fit_skew_symmetric_M`)이 바로 이 `M`을 푸는 코드입니다.

**꼭 기억할 한 줄:** jPCA는 **"무엇처럼 보이나"**만 말합니다. **"왜"는 모릅니다.** 방정식이
필요 없기 때문에 — 이게 뇌에도 쓸 수 있는 이유입니다.

---

## 3. 고정점 = "왜 도는가"의 답: 소용돌이의 '배수구' (기전)

**비유.** 욕조 물이 배수구로 빠질 때 소용돌이가 생깁니다. 물 입자들(= 궤적)이 빙빙 돕니다.
- **왜 돌까요?** 가운데 **배수구**가 있고, 그 주변 **물살(= 벡터장 `dx/dt`)**이 소용돌이
  모양이기 때문입니다.
- **배수구의 위치와 주변 물살을 알면, "왜 도는지"가 설명됩니다.**

용어를 이 비유로 옮기면:
- **고정점(fixed point)** = 물이 안 움직이는 점(속도 0) = 배수구 중심.
- **야코비안(Jacobian)** = 배수구 주변 물살의 '기울기'. 이걸 보면 주변 흐름 모양을 압니다.
- **고유값(eigenvalue)** = 그 흐름의 성질:
  - 실수 **음수** → 빨려 들어감(**안정**, attractor).
  - 실수 **양수** → 밀려남(**불안정**).
  - **복소수 `a ± bi`** → **나선/회전!** `b`가 회전 속도, `a>0`이면 밖으로 퍼지는 나선.
- 👉 노트북 **M4**: 도달 RNN에서 **`0.51 ± 1.33j`** 짜리 점을 찾습니다 = "여기 배수구가 있고,
  `1.33`의 속도로 회전시키며 밖으로 퍼진다." → **이 배수구가 M2/M3에서 본 회전을 만든다.**

---

## 4. 왜 뇌에는 이걸 못 하나? (프로젝트의 핵심 비대칭)
- 뇌에서 우리가 가진 것은 **궤적(물 입자 사진)**뿐입니다. **물살(벡터장 = 방정식)은 모릅니다.**
- 배수구를 찾으려면 물살을 알아야 하는데, 뇌의 물살(방정식)이 없습니다. → **뇌엔 고정점 분석 불가.**
- 반면 jPCA는 사진(궤적)만으로 되니까 **뇌에도 됩니다.**
- **RNN은 우리가 직접 만든 인공 신경망** → 방정식(`dx/dt`)을 압니다 → 배수구를 찾을 수 있습니다.
- 그래서 뷰어에서 **뇌 = 궤적 + 평면만**, **RNN = + 배수구(X 표시) + 물살(화살표)**.
  (뇌 패널에 X가 없는 이유가 바로 이것!)

---

## 5. 왜 RNN을, 그것도 '스파이크가 아니라 과제로' 학습하나?
- 알고 싶은 건 "뇌 회전이 **왜** 생기나"인데 뇌 방정식이 없습니다. → 뇌 대신 **같은 일(팔 뻗기)을
  하는 모델**을 만들어, 그 **안**(방정식)을 들여다봅니다.
- ⚠️ **함정:** RNN을 뇌의 스파이크 데이터에 맞추면? → 그건 "뇌를 베낀 것"이라 "닮았다"는 게
  당연해집니다(순환논법 = 무의미).
- 그래서 RNN에는 **행동 과제만** 줍니다: 입력 = 목표 방향 + go 신호, 출력 = 손 속도. **뇌
  데이터는 안 보여줍니다.**
- 그런데도 RNN이 **스스로 회전을 만들어냅니다**(M3) → 결론: **회전은 이 과제를 푸는 자연스러운
  해법이다.** 뇌를 베껴서가 아니라.

---

## 6. 플립플롭(M1)은 왜 '가장 먼저' 하나?
- 배수구 찾는 도구(고정점 탐색기)가 **진짜 잘 작동하는지**를 먼저 시험해야 합니다.
- 그래서 **정답을 아는 장난감 문제**로 검증합니다: 3비트 기억장치는 상태가 2³ = **8개** →
  8개의 안정 배수구가 **정육면체 8꼭짓점**에 있어야 합니다.
- 탐색기가 정확히 8개를 찾으면(노트북 `GATE ... True`) → 도구 신뢰 OK → 그제서야 정답을 모르는
  도달 RNN에 적용합니다.
- **교훈: 모르는 데 쓰기 전에, 아는 데서 먼저 검증한다.**

---

## 7. 전체를 관통하는 한 문장 (이걸 스스로 말할 수 있으면 다 이해한 것)
> **jPCA는 뇌(M2)와 RNN(M3) 궤적에서 회전을 *본다(기술)*. 하지만 *왜* 도는지는 방정식이 있는
> RNN에서만 물을 수 있고(M4), 그 답이 복소 고유값 `±1.33j`를 가진 회전 안장점(배수구) — 이것이
> 회전을 *생성한다(기전)*.**

---

## 8. 노트북 마일스톤별 "이것만 이해하면 됨" + 자가 체크

| 구간 | 무엇을 보나 | 이것만 이해 | 자가 체크 (답을 스스로 말해보기) |
|---|---|---|---|
| 로드맵·§0 | 마크다운 | jPCA=기술 / 고정점=기전 | 왜 뇌엔 고정점 분석을 못 쓰나? |
| **M0** | 🔍 `fit_skew_symmetric_M` | 왜 그냥 최소제곱이 아니라 **반대칭** 제약? | 답: 늘이기(감쇠)를 빼고 **순수 회전만** 남기려고 |
| **M1** | `GATE ... True` + 뷰어 | **정답 아는** 문제로 도구 보정 → 8꼭짓점 | 8개 배수구는 무슨 계산? (3비트 기억) |
| **M2** | `R2 0.52 / 1.37 Hz` + no-CCM | 실제 뇌도 회전; 전처리 빼도 회전은 **살아남음** | 그 전처리는 회전을 *만드나*, *고르나*? |
| **M3** | `velocity R2 0.9989 → ROTATE` | **과제로만** 학습했는데 회전 창발 | 이게 왜 "베낀 게 아님"의 증거인가? |
| **M4** ⭐ | `saddle spiral 0.51+1.33j` + 흐름장 | 복소 고유값이 **회전을 생성** | M2의 뇌 회전과 어떻게 연결되나? |
| **M5** | 뷰어 Side-by-side | 뇌엔 X 없음, RNN엔 있음 | 왜 뇌 패널엔 배수구가 없나? |

막히는 칸이 있으면, 그 셀의 출력이나 질문을 그대로 물어보세요 — 그 자리에서 더 풀어 설명해 드립니다.
